export type { SortOption } from "@/components/igm/types";
export type { Issue, IssueArticle } from "@/components/igm/types";

// Keep these for backward compatibility
export type SortOptionType = "latest" | "year" | "downloads" | "shares";
