export interface Founder {
  name: string;
  title: string;
  role: string;
  image?: string;
  isDeceased?: boolean;
  bio?: string;
  specialties?: string[];
  location?: string;
  achievements?: string[];
}