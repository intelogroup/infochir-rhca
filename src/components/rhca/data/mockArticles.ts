
import { RhcaArticle } from "../types";
import { formatDateForFilename } from "@/lib/utils";

// This file is kept for compatibility but no longer provides mock data
// All data is now fetched directly from the Supabase database
export const mockRhcaArticles: RhcaArticle[] = [];
